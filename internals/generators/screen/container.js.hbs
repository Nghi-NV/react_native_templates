/**
* Author: {{ author }}
* Email: {{ email }}
*/

import React, { PureComponent } from 'react';
{{#if hasRedux}}
import { connect } from 'react-redux'
{{/if}}
{{#if hasMultiScreen}}
import ViewPortrait from '../../screens/{{ lowerCase name }}/index-portrait.js'
import ViewLandscape from '../../screens/{{ lowerCase name }}/index-landscape.js'
{{else}}
import ViewComponent from '../../screens/{{ lowerCase name }}'
{{/if}}

class {{ properCase name }} extends PureComponent { 
  render() {
    return (
      {{#if hasMultiScreen}}
      <ViewPortrait {...this.props} />
      {{else}}
      <ViewComponent {...this.props} />
      {{/if}}
    );
  }
}

{{#if hasRedux}}
const mapDispatchToProps = {
}

const mapStateToProps = (state) => {
  return {
  }
}

export default connect(mapStateToProps, mapDispatchToProps)({{ properCase name }})
{{else}}
export default {{ properCase name }};
{{/if}}

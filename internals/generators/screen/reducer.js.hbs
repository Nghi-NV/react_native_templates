/**
*
* Author: {{ author }}
* Email: {{ email }}
*
*/

import {
  DEFAULT_ACTION,
} from '../actions/{{ lowerCase name }}';

const initialState = {};

function {{ camelCase name }}Reducer(state = initialState, action) {
  switch (action.type) {
    case DEFAULT_ACTION:
      return state;
    default:
      return state;
  }
}

export default {{ camelCase name }}Reducer;

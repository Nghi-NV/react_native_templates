/**
*
* Author: {{ author }}
* Email: {{ email }}
*
*/

import { takeLatest, put, call } from "redux-saga/effects";

function* _default(action) {

};

export function* watchInitial() {
  yield takeLatest(DEFAULT_ACTION, _default)
};
